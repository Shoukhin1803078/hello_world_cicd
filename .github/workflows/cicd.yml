name: CI/CD learning

on:
  push:
    branches:
      - main

jobs:
  ci_part:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/helloworld:latest .

      - name: Push that image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/helloworld:latest

  cd_part:
    name: Deploy to Server
    needs: ci_part
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_SSH_KEY }}

          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/helloworld:latest

            docker stop helloworld || true
            docker rm helloworld || true

            docker run -d --name helloworld --restart unless-stopped -p 8000:8000 ${{ secrets.DOCKER_USERNAME }}/helloworld:latest